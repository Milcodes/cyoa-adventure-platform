# This is a TEMPLATE file. Do NOT commit actual secrets to git!
# Create actual secrets using kubectl:
# kubectl create secret generic cyoa-secrets --from-literal=POSTGRES_PASSWORD=xxx --namespace=cyoa-platform

apiVersion: v1
kind: Secret
metadata:
  name: cyoa-secrets
  namespace: cyoa-platform
type: Opaque
stringData:
  # Database Credentials
  POSTGRES_USER: "cyoa_user"
  POSTGRES_PASSWORD: "CHANGE_ME_STRONG_PASSWORD"
  DATABASE_URL: "postgresql://cyoa_user:CHANGE_ME_STRONG_PASSWORD@postgresql-service:5432/cyoa_platform"

  # Redis Password
  REDIS_PASSWORD: "CHANGE_ME_REDIS_PASSWORD"

  # JWT Secrets
  JWT_SECRET: "CHANGE_ME_JWT_SECRET_MINIMUM_32_CHARS_LONG"
  JWT_REFRESH_SECRET: "CHANGE_ME_JWT_REFRESH_SECRET_MINIMUM_32_CHARS_LONG"

  # MinIO Credentials
  MINIO_ROOT_USER: "minioadmin"
  MINIO_ROOT_PASSWORD: "CHANGE_ME_MINIO_PASSWORD"
  MINIO_ACCESS_KEY: "CHANGE_ME_ACCESS_KEY"
  MINIO_SECRET_KEY: "CHANGE_ME_SECRET_KEY"

  # Email (SendGrid)
  SENDGRID_API_KEY: "SG.xxxxxxxxxxxxx"
  EMAIL_FROM: "noreply@cyoa.yourdomain.com"

  # Push Notifications
  VAPID_PUBLIC_KEY: "CHANGE_ME_VAPID_PUBLIC_KEY"
  VAPID_PRIVATE_KEY: "CHANGE_ME_VAPID_PRIVATE_KEY"
